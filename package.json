{
  "name": "effect-flow",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@effect/vitest": "4.0.0-rc.115",
    "@types/bun": "latest",
    "vitest": "^5.0.1",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "test": "bunx --bun vitest run",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "4.0.0-rc.115"
  }
}
