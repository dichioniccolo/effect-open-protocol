{
  "name": "@effect-open-protocol/cli",
  "version": "0.0.0",
  "description": "Controller simulator and client CLIs that record Open Protocol wire traces, plus the chaos demo and soak run.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bunx --bun vitest run"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-rc.115",
    "@effect/sql-sqlite-bun": "4.0.0-rc.115",
    "@effect-open-protocol/store": "workspace:*",
    "effect": "4.0.0-rc.115",
    "effect-open-protocol": "workspace:*"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-rc.115",
    "@types/bun": "latest",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
