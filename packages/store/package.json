{
  "name": "@effect-open-protocol/store",
  "version": "0.0.0",
  "description": "SQLite schema, migrations and queries for recorded wire traces.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bunx --bun vitest run"
  },
  "dependencies": {
    "effect": "4.0.0-rc.115",
    "effect-open-protocol": "workspace:*"
  },
  "devDependencies": {
    "@effect/sql-sqlite-bun": "4.0.0-rc.115",
    "@effect/vitest": "4.0.0-rc.115",
    "@types/bun": "latest",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
